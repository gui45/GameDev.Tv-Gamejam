using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PlayerStates
{
    IDLE = 0,
    MOVING = 1,
    OFFGROUND = 2,
    FALLING = 3,
    ATTACKING = 4,
    DYING = 5
}
