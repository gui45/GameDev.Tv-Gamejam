using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PlayerGhostStates
{
    IDLE = 0,
    MOVING = 1,
    OFFGROUND = 2,
    FALLING = 3,
    DYING = 4,
    NOTGHOST = 5
}
